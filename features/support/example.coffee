###module.exports = ()->
  @Given /^the setup$/, (callback) ->
    # express the regexp above with the code you wish you had
    callback.pending()
    return
  @When /^the cause occurs$/, (callback) ->
    # express the regexp above with the code you wish you had
    callback.pending()
    return
  @Then /^effect triggers$/, (callback) ->
    # express the regexp above with the code you wish you had
    callback.pending()
    return
###